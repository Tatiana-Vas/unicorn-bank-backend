# 🦄 Unicorn Bank — Backend Architecture (MVP)

Unicorn Bank — это семейное финансовое приложение для детей и родителей. Родители управляют заданиями, депозитами и запросами, дети учатся зарабатывать, копить и принимать финансовые решения.

Документ описывает **архитектуру данных и доменную модель MVP**. Это опорная точка для backend-разработки.

---

## 🎯 Цели MVP

- Управление финансами ребёнка (spending / saving)
- Мотивация через задания (tasks)
- Финансовые цели (deposits, wishlist)
- Прозрачность: откуда пришли деньги
- Асинхронное взаимодействие родитель ↔ ребёнок через requests

---

## 👥 Пользователи

### Parent

- Управляет аккаунтами детей
- Создаёт и подтверждает задания
- Создаёт депозиты
- Пополняет spending account
- Обрабатывает запросы ребёнка

### Child

- Имеет собственный логин (email)
- Выполняет задания
- Видит баланс и историю денег
- Создаёт запросы
- Управляет wishlist

Поддерживается модель **нескольких родителей для одного ребёнка**.

---

## 🧱 Основные домены

### 1. Users

- `parent`
- `child`
- `parent_child` (many-to-many связь)

Хранят персональные данные, аватары и настройки персонализации.

---

### 2. Accounts

#### Spending Account

- Один на ребёнка
- Используется для трат и вознаграждений
- Всегда имеет **историю операций** (transactions):
  - списания
  - пополнения
- Баланс рассчитывается как сумма транзакций

#### Saving Account (Deposit)

- Может быть несколько
- Имеет срок и процентную ставку
- Проценты **не хранятся**, а вычисляются
- В детальной информации показываются:
  - `current_amount` — вложение + начисленные проценты на текущую дату
  - `projected_amount` — сумма на конец срока при полном удержании депозита

---

### 3. Tasks

#### Task (шаблон)

- One-time или recurring
- Определяет правила выполнения
- Содержит `due_rule` вместо event:
  - one_time: `due_at` (datetime)
  - recurring: `schedule` (cron / rrule) и `grace_period`

#### Task Instance

- Конкретное выполнение, сгенерированное из Task
- Имеет:
  - `expected_at` — когда задание должно быть выполнено
  - `expired_at` — вычисляемое время истечения (expected_at + grace)
- Имеет собственный статус
- Используется для истории и выплат

Статусы:

- new
- pending_approval
- approved
- rejected
- expired

---

### 4. Requests

Единый механизм запросов от ребёнка к родителю:

- новый таск
- апрув выполненного таска
- новый депозит
- покупка
- наличные

Родитель обрабатывает requests асинхронно.

---

### 5. Transactions

**Источник правды о деньгах.**

Любое движение средств — это транзакция:

- подарок
- бонус
- карманные деньги
- выплата за таск
- проценты по депозиту
- покупка / наличные

Баланс счёта = сумма транзакций.

---

### 6. Wishlist

- Список желаний ребёнка
- Не всегда связан с деньгами
- Может быть выполнен или запрошен

---



### 8. Notifications

Факты событий, которые должны быть показаны пользователю:

- новые запросы
- новые задания
- выплаты
- закрытие депозитов

Push-логика находится на уровне приложения, БД хранит только факт.

---

## 🧠 Архитектурные принципы

- **Balance — не источник правды**
- **Проценты не хранятся**
- **Повторяющиеся задачи = task + task\_instance**
- **Requests — единый механизм взаимодействия**
- **Схема готова к масштабированию (school, expenses)**

---

## 🗂 Структура проекта

```
unicorn-bank/
 ├─ schema.sql      # SQL схема БД
 ├─ README.md       # Архитектура и домены
```

---

## 🚀 Следующие шаги

- API контракты (REST / GraphQL)
- Выбор backend stack
- Аутентификация и авторизация
- Тестовые данные и сценарии

---

🦄 Unicorn Bank — система, которая учит детей деньгам, а не просто считает их.

